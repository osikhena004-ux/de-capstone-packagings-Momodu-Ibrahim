# 🛒 OmniCart Pipeline

**OmniCart Pipeline** is a production-ready, command-line data pipeline that fetches, enriches, and analyzes product and seller data from multiple API sources.  
Built for **OmniCart Analytics**, this tool allows data engineers and analysts to run a complete end-to-end data workflow — from extraction to transformation to analysis — with a single terminal command.

---

## 🚀 Overview

OmniCart Pipeline automates the process of:
1. **Fetching** product and seller data from public APIs.
2. **Enriching** the data by merging related datasets.
3. **Analyzing** seller performance metrics.
4. **Exporting** a JSON report summarizing key business insights.

The tool is fully packaged as a **CLI (Command Line Interface)** using [Poetry](https://python-poetry.org/), making it easy to install and execute from anywhere:

```bash
pip install omnicart-pipeline-yourname
omnicart-pipeline
# de-capstone-packagings-Momodu-Ibrahim
